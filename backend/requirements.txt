# --- Web framework / server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Computer vision / detection ---
# CPU build of torch keeps the image small for EC2; swap for a CUDA build if
# the instance has a GPU. opencv-python-headless avoids GUI deps in Docker.
ultralytics==8.3.55
opencv-python-headless==4.10.0.84
numpy==2.1.3

# --- Utilities ---
python-multipart==0.0.20
